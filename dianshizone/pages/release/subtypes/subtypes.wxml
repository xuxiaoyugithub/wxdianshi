<block wx:for="{{subtypes}}">
  <!-- <navigator url="../release?id={{item.id}}&name={{item.name}}" bindtap="jump" data-id="{{item.id}}" data-name="{{item.name}}"> -->
    <view class="types" bindtap="jump" data-id="{{item.id}}" data-name="{{item.name}}">{{item.name}}</view>
  <!-- </navigator> -->
</block>